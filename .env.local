# VITE_API_BASE_URL is intentionally unset here.
# The app derives the backend URL from window.location.hostname at runtime,
# so it works from any device on the LAN without manual changes.
#
# Uncomment below only if you need to force a specific host:
# VITE_API_BASE_URL=http://192.168.1.42:3001
